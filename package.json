{
  "name": "email-validation-app",
  "version": "1.0.0",
  "description": "Validate the value of the email field using Mailboxlayer",
  "dependencies": {
    "axios": "^0.17.1",
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "lint": "eslint src/components*",
    "server": "node server/server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn start\""
  },
  "devDependencies": {
    "concurrently": "^3.5.0",
    "eslint-config-airbnb-standard": "^1.6.6"
  },
  "proxy": "http://localhost:5000/",
  "license": "ISC"
}
